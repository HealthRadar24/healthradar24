name: Seed Freshness Monitor

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  statuses: read

jobs:
  monitor:
    # HealthRadar intentionally operates only the relay while inherited seed
    # capabilities are deferred. Keep manual diagnostics available, but do not
    # page every 15 minutes for a full fleet this fork does not claim to run.
    if: github.event_name == 'workflow_dispatch' || github.repository != 'HealthRadar24/healthradar24'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24'

      - name: Require green main for scheduled alerts
        id: gate
        if: github.event_name == 'schedule'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gate_state=$(gh api "repos/$GITHUB_REPOSITORY/commits/$GITHUB_SHA/status" \
            --jq '[.statuses[] | select(.context == "gate")] | sort_by(.updated_at) | last | .state // "missing"')
          echo "gate_state=$gate_state"
          if [ "$gate_state" = "success" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
            echo "Skipping seed freshness alert because main gate is $gate_state."
          fi

      - name: Check ingestion operational acceptance
        if: github.event_name == 'workflow_dispatch' || steps.gate.outputs.should_run == 'true'
        env:
          HEALTH_URL: ${{ vars.WM_HEALTH_URL }}
        run: node scripts/check-seed-freshness.mjs
